{
  "name": "traveltrack-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend-react"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend-react",
    "vercel-build": "npm run build --workspace=frontend-react",
    "dev:frontend": "npm run dev --workspace=frontend-react"
  }
}
