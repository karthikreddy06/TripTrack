{
  "name": "traveltrack-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend-react"
  ],
  "scripts": {
    "build": "cd frontend-react && npm install && npm run build",
    "vercel-build": "cd frontend-react && npm install && npm run build",
    "dev:frontend": "npm run dev --prefix frontend-react"
  }
}
